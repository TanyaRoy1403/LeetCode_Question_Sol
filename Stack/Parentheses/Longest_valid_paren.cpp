#include<iostream>
#include<stack>
#include<vector>
using namespace std;

int longestValidParentheses(string s) {
    stack<char>st;
    st.push(-1);
    for(int i=0;i<s.length();i++){
        char ch=s[i];
        
    }
        
}