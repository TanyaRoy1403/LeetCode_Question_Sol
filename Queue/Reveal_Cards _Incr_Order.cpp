#include<iostream>
#include<vector>
#include<queue>
#include<algorithm>
using namespace std;

//approach 1
vector<int> revealdeck(vector<int>arr){
    int n=arr.size();
    bool flag=true;
    sort(arr.begin(),arr.end());
    for(int i=0;i<n;i++){
        if()
    }
}
